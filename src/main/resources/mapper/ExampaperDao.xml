<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yb.dao.ExampaperDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.yb.entity.ExampaperEntity" id="exampaperMap">
        <result property="name" column="name"/>
        <result property="time" column="time"/>
        <result property="status" column="status"/>
        <result property="examnum" column="examnum"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.yb.entity.vo.ExampaperVO" >
		SELECT * FROM exampaper  exampaper         
        <where>${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.yb.entity.vo.ExampaperVO" >
		SELECT  exampaper.* FROM exampaper  exampaper 	
 		<where>${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.yb.entity.view.ExampaperView" >

		SELECT  exampaper.* FROM exampaper  exampaper 	        
        <where>${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.yb.entity.view.ExampaperView" >
		SELECT * FROM exampaper  exampaper <where>${ew.sqlSegment}</where>
	</select>
	


</mapper>
