<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yb.dao.ForumDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.yb.entity.ForumEntity" id="forumMap">
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="parentid" column="parentid"/>
        <result property="username" column="username"/>
        <result property="avatarurl" column="avatarurl"/>
        <result property="isdone" column="isdone"/>
        <result property="istop" column="istop"/>
        <result property="toptime" column="toptime"/>
        <result property="cover" column="cover"/>
        <result property="isanon" column="isanon"/>
        <result property="delflag" column="delflag"/>
        <result property="sfsh" column="sfsh"/>
        <result property="shhf" column="shhf"/>
        <result property="userid" column="userid"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.yb.entity.vo.ForumVO" >
		SELECT * FROM forum  forum         
        <where>${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.yb.entity.vo.ForumVO" >
		SELECT  forum.* FROM forum  forum 	
 		<where>${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.yb.entity.view.ForumView" >

		SELECT  forum.* FROM forum  forum 	        
        <where>${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.yb.entity.view.ForumView" >
		SELECT * FROM forum  forum <where>${ew.sqlSegment}</where>
	</select>
	


</mapper>
